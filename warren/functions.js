//function multiplier_thing(number1,number2) {
   // let new_number = number1 * number2;
  //  return new_number;
//}
//console.log(multiplier_thing(5,8))

//var some_number = 5;
//var Dan = multiplier_thing(some_number);

//console.log(Dan);

function email_check(email) {
    if (email)
  return `Nice ${email} let's see whats new`;
}